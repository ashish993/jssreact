"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createDisconnectedAssetMiddleware = void 0;
var fs_1 = __importDefault(require("fs"));
var path_1 = __importDefault(require("path"));
/**
 * @param {DisconnectedAssetMiddlewareOptions} config
 */
function createDisconnectedAssetMiddleware(_a) {
    var manifestPath = _a.manifestPath, staticRootPath = _a.staticRootPath;
    return function disconnectedAssetMiddleware(request, response) {
        var localUrl = request.originalUrl;
        // strip query
        if (localUrl.indexOf('?') > -1) {
            localUrl = localUrl.substring(0, localUrl.indexOf('?'));
        }
        // match on in-manifest assets
        var localPath = path_1.default.join(manifestPath, localUrl);
        if (!fs_1.default.existsSync(localPath)) {
            // match on static app assets
            localPath = path_1.default.join(staticRootPath, localUrl);
            console.log("> [STATIC] '" + request.originalUrl + "' - sent " + localPath + " from static assets");
        }
        else {
            console.log("> [MEDIA] '" + request.originalUrl + "' - sent " + localPath + " from manifest");
        }
        response.sendFile(localPath);
    };
}
exports.createDisconnectedAssetMiddleware = createDisconnectedAssetMiddleware;
