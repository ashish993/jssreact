import { GeneratePlaceholdersPipelineArgs } from '../../manifest.types';
declare const _default: (args: GeneratePlaceholdersPipelineArgs) => {
    placeholderNames: string[];
    items: import("../../manifest.types").RouteDefinition[];
    renderings: any[];
    placeholders: import("../../manifest.types").PlaceholderDefinition[];
    rootPlaceholders: string[];
    skipPlaceholderBlacklist: boolean;
    pipelines: {
        [key: string]: import("@sitecore-jss/sitecore-pipelines").ExecutablePipeline;
    };
};
export default _default;
