import { INgrokOptions } from 'ngrok';
export interface TunnelOptions extends INgrokOptions {
    port?: number;
    quiet?: boolean;
}
/**
 * @param {string} renderHostname
 * @param {INgrokOptions} options
 */
export declare function startRenderHostTunnel(renderHostname: string, options?: INgrokOptions): Promise<string>;
